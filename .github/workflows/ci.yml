name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Run tests
        run: swift test

  screenshots:
    name: screenshots
    if: github.event_name == 'pull_request'
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - name: Render screenshots + sync README
        run: GEN_SCREENSHOTS=1 swift test --filter ScreenshotGenTests
      - name: Commit generated assets
        run: |
          if [ -n "$(git status --porcelain Docs/img README.md)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add Docs/img README.md
            git commit -m "docs: regenerate README screenshots"
            git push
          else
            echo "No screenshot/README changes."
          fi
